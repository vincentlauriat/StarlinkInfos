# ── macOS ──────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── Xcode ──────────────────────────────────────────────
build/
DerivedData/
*.xcuserstate
*.xcscmblueprint
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Release artifacts (published on the Releases page, not versioned)
*.dmg
dmg-staging/
release/

# Sparkle CLI tools (downloaded by release.sh) and the EdDSA private-key backup
# — NEVER commit the private key. See Scripts/release.sh header for details.
.sparkle-tools/
.sparkle-tools-backup/

# Project generated by XcodeGen — regenerate with `xcodegen generate`
*.xcodeproj/

# ── Swift Package Manager ──────────────────────────────
.build/
.swiftpm/

# ── Tooling / local state (not for publication) ────────
.omc/
.claude/

# ── Private working docs (kept local, may contain personal info) ─
CLAUDE.md
COMMANDS.md
MEMORY.md
PLAN.md
TODOS.md
